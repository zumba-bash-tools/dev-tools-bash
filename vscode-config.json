{
	"version": "0.2.0",
	"configurations": [
		{
			"name": "Listen for XDebug",
			"type": "php",
			"request": "launch",
			"port": %s,
			"pathMappings": {
				"/var/www/%s/releases/local_source": "${workspaceRoot}"
			}
		}
	]
}
